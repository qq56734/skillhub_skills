# LICENSE-NOTE for ios-application-dev

## 协议声明来源

`SKILL.md` frontmatter 自带声明：
```
license: MIT
metadata:
  author: MiniMax-OpenSource
```

## 风险说明

- 声称的源代码仓库为 `MiniMax-AI/AgentSkills`。
- 本审查（2026-08-18）实测该仓库 `main` 与 `master` 分支均返回 404，**无法独立验证 LICENSE 文本**。
- 本目录中的 `SKILL.md` 与 9 个 `references/*.md` 文档均为该作者原创或整理的内容，frontmatter 中包含完整的 MIT 条款引用，但本目录未附带独立的 `LICENSE` 文本。

## 上传建议

1. **若可接受风险**（如内部使用）：可按 `SKILL.md` 中 frontmatter 的 `license: MIT` 声明入库。
2. **若需对外发布**：建议先通过 `MiniMax-OpenSource` 在 clawhub 或其他渠道联系作者，要求其提供：
   - 有效的公开仓库地址
   - 仓库根目录的 `LICENSE` 文件
3. 入库时建议在 SkillHub 元数据中保留本 `LICENSE-NOTE.md`，并标记为 `caution` 状态。

## 审查记录

详细审查过程见 `C:\Users\qq567\Desktop\codes\projects\ace_skills\_review_41_81\review_report_41_81.md` 第 35-37 行。
